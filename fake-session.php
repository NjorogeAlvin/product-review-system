<?php
// session_start();

// $mode = $_GET['as'] ?? 'admin';

// if ($mode === 'logout') {
//     session_destroy();
//     echo "Session cleared. You're now logged out.";
//     exit;
// }

// $_SESSION['user_id'] = ($mode === 'reviewer') ? 2 : 1;
// $_SESSION['role'] = $mode;

// echo "Session set as: " . $mode;

